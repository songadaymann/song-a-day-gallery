{
  "name": "song-a-day-gallery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.js",
    "preview": "vite preview",
    "preview:cloudflare": "npx -y wrangler@4.130.0 dev",
    "deploy:cloudflare": "npx -y wrangler@4.130.0 deploy"
  },
  "devDependencies": {
    "dotenv": "^16.5.0",
    "lunr": "^2.3.9",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "algoliasearch": "^4.24.0"
  }
}
